version: '3.8'

services:
  postgres:
    image: postgres:13.21-alpine3.20
    container_name: postgres
    restart: always
    env_file:
      - .env
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - '5432:5432'
